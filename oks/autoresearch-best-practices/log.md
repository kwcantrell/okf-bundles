# Change Log — AutoResearch Best Practices bundle

2026-07-07 — bundle created

Initial authoring of the `autoresearch-best-practices` OKF bundle, starting with
the `loop/` area. This area documents the mechanics of Andrej Karpathy's
[autoresearch](https://github.com/karpathy/autoresearch) autonomous-ML-research
loop:

- Added the `loop/` area `index.md` (progressive-disclosure entry point listing
  the four loop concepts) and this `log.md`.
- Authored **loop** (the agent research loop, the spec/script split, single-file
  discipline, and keep-or-revert with git) — 4 concepts.

Every concept is grounded in the autoresearch repository's own files (`README.md`,
`program.md`, `train.py`). Subsequent tasks append lines here as further areas
are added.
