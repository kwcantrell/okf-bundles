# Change Log — Git Best Practices bundle

2026-07-07 — bundle created

Initial authoring of the `git-best-practices` OKF bundle. This entry covers the
first five concept areas and the bundle entry points:

- Added the bundle `index.md` (progressive-disclosure entry point listing all
  ten planned concept areas) and this `log.md`.
- Authored **fundamentals** (repository model, staging area, refs and objects).
- Authored **workflows** (GitHub flow, Git Flow, trunk-based development,
  forking workflow).
- Authored **branching** (naming conventions, branch protection, merge vs.
  rebase, resolving conflicts).
- Authored **commits** (atomic commits, Conventional Commits, commit-message
  style, signing commits).
- Authored **collaboration** (pull requests, code review, draft PRs,
  CODEOWNERS).

The remaining five areas (history, remote, automation, security, scale) are
planned for a subsequent authoring pass, which will also add back-links from
those areas into the concepts above.
