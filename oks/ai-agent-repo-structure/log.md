# Change Log — AI Agent Repo Structure bundle

2026-07-07 — bundle created

Initial authoring of the `ai-agent-repo-structure` OKF bundle. This entry covers
all six concept areas and the bundle entry points:

- Added the bundle `index.md` (progressive-disclosure entry point listing all
  six concept areas) and this `log.md`.
- Authored **context-files** (`AGENTS.md` and `CLAUDE.md`, progressive
  disclosure, precedence and scope).
- Authored **skills** (what a skill is, the `SKILL.md` file format, skill
  placement, project vs. personal skills).
- Authored **conventions** (directory layout, naming for discoverability,
  `AGENTS.md` as a README for agents).
- Authored **knowledge** (OKF bundles in repos, docs written for agents,
  machine-readable metadata).
- Authored **tooling** (MCP and tool config, slash commands and hooks).
- Authored **practices** (determinism and reproducibility, guardrails and
  permissions).

The bundle cross-links into the sibling `git-best-practices` bundle where
natural — notably from `knowledge/okf-bundles-in-repos`, which points to the
version-control practices that keep a committed knowledge bundle reviewable.
