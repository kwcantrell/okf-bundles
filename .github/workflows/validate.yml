name: Validate OKF bundles

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Validate OKF bundles and cross-references
        run: uv run tools/validate_okf.py
